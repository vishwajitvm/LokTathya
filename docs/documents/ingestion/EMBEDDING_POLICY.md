# Embedding Policy
Embeddings are NOT generated for structured DB records (e.g., budget integers or election vote counts).
Vector embeddings are strictly reserved for narrative documents (PDFs, policies, orders) processed via MinIO/pgvector.
