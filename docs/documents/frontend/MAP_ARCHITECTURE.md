# Design System
Reusable components (`components/ui/`) govern buttons, inputs, tables, cards, badges, and empty/loading states.
Empty values are NEVER rendered as zero; instead, `PARTIAL`, `INSUFFICIENT_DATA`, and `UNKNOWN` are styled with explicit visual states.
