# Citation Component
Every factual claim relies on the `<Citation />` component to render the `CitationDTO`. It visually links the user to the `officialUrl` and explicitly states the `retrievedAt` timestamp.
