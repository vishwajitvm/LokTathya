# Report Reproducibility
Generated reports act as historical snapshots. The JSON structure embeds the exact `content_versions` and `metric_versions` used, ensuring the report can be perfectly reproduced computationally even if definitions change years later.
