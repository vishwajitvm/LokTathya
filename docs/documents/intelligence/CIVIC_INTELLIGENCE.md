# Neutral Language Policy
LokTathya prohibits defamatory or speculative language. 
Financial gaps are labelled `unreconciled` or `data discrepancy`. The terms `missing money`, `fraud`, or `corruption` are strictly banned unless an authoritative legal source is explicitly cited supporting that claim.
