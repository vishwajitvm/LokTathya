# Model Registry & Evaluation
No LLM is permitted to predict election winners.
Models must outperform deterministic baselines (e.g., `Previous Winner`, `Incumbency Status`).
If a model fails to consistently beat a baseline, its status is marked `REJECTED`.
