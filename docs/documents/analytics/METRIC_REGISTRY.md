# Metric Registry
All derived metrics are explicitly registered (e.g. `METRIC-FIN-UTILIZATION-001`). This ensures reproducible, version-controlled definitions for every percentage, rate, or aggregation exposed by the API.
