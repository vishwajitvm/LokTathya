# Citation API
Every factual endpoint must embed a `CitationDTO` mapping output back to its authoritative source, page, and content version. This prevents AI hallucination interfaces from stripping the original government source.
