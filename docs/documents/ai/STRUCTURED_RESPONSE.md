# Civic AI Architecture
This is a grounded question-answering interface, NOT a generic chatbot.
The LLM operates strictly over deterministic analytics, canonical data, and verified documents.
The LLM cannot execute raw SQL; it utilizes predefined typed tools.
