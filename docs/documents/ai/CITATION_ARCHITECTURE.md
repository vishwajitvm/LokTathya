# Citation Architecture & Version Awareness
Retrieved chunks output full citation paths: `[Source Name](Official URL) -> Document -> ContentVersion -> Page`.
Historical content versions are never overwritten; retrieval filters distinguish `LATEST` vs `SUPERSEDED` versions natively.
