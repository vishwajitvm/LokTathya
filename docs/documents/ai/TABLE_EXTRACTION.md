# Table Extraction
Tables are structurally modeled, not flattened into text. 
Rows and columns are retained alongside document metadata. Complex hierarchical tables are marked with structural caveats during extraction.
